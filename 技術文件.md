# Cue Sheet Pro 技術文件

## 📋 目錄

1. [專案概述](#專案概述)
2. [系統架構](#系統架構)
3. [模組說明](#模組說明)
4. [配置管理](#配置管理)
5. [安裝與部署](#安裝與部署)
6. [資料流程](#資料流程)
7. [維護指南](#維護指南)
8. [開發指南](#開發指南)
9. [常見問題](#常見問題)
10. [API 參考](#api-參考)

---

## 專案概述

### 專案名稱
**Cue Sheet Pro (媒體排程生成系統)**

### 專案用途
協助業務人員生成媒體排程表 (Cue表)，支援：
- HTML 預覽
- Excel 報表生成（支援三種格式：東吳、聲活、鉑霖）
- PDF 轉檔
- Ragic 資料庫整合（CRUD 操作）

### 技術棧
- **前端框架**: Streamlit
- **資料處理**: Pandas
- **Excel 處理**: openpyxl
- **PDF 轉檔**: LibreOffice CLI
- **外部 API**: Google Sheets API (gviz), Ragic API
- **語言**: Python 3.x

### 系統依賴
- Google Sheet（作為設定檔來源）
- LibreOffice（用於 PDF 轉檔）
- Ragic 資料庫（用於資料儲存）

---

## 系統架構

### 模組化架構

```
cueapp/
├── app.py                 # 主程式入口（Streamlit UI）
├── config.py              # 配置管理（常數、URL、API Key）
├── utils.py               # 基礎工具函式
├── data_loader.py         # Google Sheet 資料讀取
├── calculator.py          # 排程運算核心邏輯
├── html_generator.py      # HTML 預覽生成
├── excel_renderer.py      # Excel 報表生成引擎
├── pdf_converter.py       # PDF 轉檔功能
├── ragic_api.py          # Ragic API 整合
└── requirements.txt       # Python 套件依賴
```

### 資料流向圖

```
使用者輸入 (app.py)
    ↓
資料驗證與處理 (utils.py)
    ↓
載入設定檔 (data_loader.py) ← Google Sheet
    ↓
排程計算 (calculator.py)
    ↓
生成報表 (excel_renderer.py, html_generator.py)
    ↓
PDF 轉檔 (pdf_converter.py) ← LibreOffice
    ↓
上傳至 Ragic (ragic_api.py) → Ragic 資料庫
```

---

## 模組說明

### 1. `app.py` - 主程式入口

**職責**: Streamlit 應用程式主入口，處理 UI 邏輯與使用者互動

**主要功能**:
- 主管登入驗證
- 使用者輸入表單（客戶資訊、預算、日期等）
- 媒體投放設定（全家廣播、新鮮視、家樂福）
- Ragic 搜尋與載入舊專案
- 檔案下載（Excel、PDF）
- Ragic 資料上傳

**關鍵 Session State**:
- `is_supervisor`: 主管權限開關
- `cb_rad`, `cb_fv`, `cb_cf`: 媒體啟用狀態
- `rad_share`, `fv_share`, `cf_share`: 預算佔比
- `ragic_url`, `ragic_key`: Ragic 連線設定

**依賴模組**:
- 所有其他模組

---

### 2. `config.py` - 配置管理

**職責**: 集中管理所有系統常數與設定

**主要配置項**:

#### 外部資源連結
- `GSHEET_SHARE_URL`: Google Sheet 設定檔連結
- `BOLIN_LOGO_URL`: 鉑霖 Logo 下載連結

#### Ragic 設定
- `RAGIC_FIELD_SERIAL`: Cue表編號欄位 ID（重要！）
- `DEFAULT_RAGIC_URL`: Ragic 表單網址
- `DEFAULT_RAGIC_KEY`: Ragic API Key
- `RAGIC_MAP`: 欄位 ID 對照表（集中管理所有欄位 ID）

#### 系統安全
- `SUPERVISOR_PASSWORD`: 主管登入密碼

#### Excel 樣式
- `FONT_MAIN`: 字型名稱
- `BS_THIN`, `BS_MEDIUM`, `BS_HAIR`: 邊框樣式
- `FMT_MONEY`, `FMT_NUMBER`: Excel 數字格式

#### 業務邏輯
- `REGIONS_ORDER`: 區域順序
- `REGION_DISPLAY_MAP`: 區域顯示名稱對照
- `DURATIONS`: 可用秒數選項

**⚠️ 重要維護點**:
- 若 Ragic 表單欄位變更，需更新 `RAGIC_MAP`
- 若 Cue表編號欄位變更，需更新 `RAGIC_FIELD_SERIAL`（可透過除錯模式確認）

---

### 3. `utils.py` - 基礎工具函式

**職責**: 提供通用工具函式

**主要函式**:

| 函式名稱 | 功能 | 參數 | 回傳值 |
|---------|------|------|--------|
| `parse_count_to_int(x)` | 將字串（含逗號）轉為整數 | `x`: 任意型別 | `int` |
| `safe_filename(name)` | 移除檔名非法字元 | `name`: 字串 | `str` |
| `html_escape(s)` | HTML 特殊字元轉義 | `s`: 字串 | `str` |
| `region_display(region)` | 區域簡寫轉完整名稱 | `region`: 字串 | `str` |
| `get_sec_factor(media, sec, factors)` | 取得秒數加成係數 | `media`: 媒體類型<br>`sec`: 秒數<br>`factors`: 係數字典 | `float` |
| `calculate_schedule(total, days)` | 排程分配演算法 | `total`: 總檔次<br>`days`: 天數 | `list[int]` |
| `get_remarks_text(...)` | 生成標準合約備註文字 | `sign_deadline`, `billing_month`, `payment_date` | `list[str]` |
| `format_campaign_details(config)` | 格式化媒體設定為字串 | `config`: 媒體設定字典 | `str` |

---

### 4. `data_loader.py` - Google Sheet 資料讀取

**職責**: 從 Google Sheet 讀取設定檔資料

**主要函式**:
- `load_config_from_cloud(share_url)`: 讀取所有設定檔

**讀取的 Sheet**:
1. **Stores**: 店舖數資料
   - `Key`: 區域識別碼
   - `Display_Name`: 顯示名稱
   - `Count`: 店舖數量

2. **Factors**: 秒數係數表
   - `Media`: 媒體類型
   - `Seconds`: 秒數
   - `Factor`: 加成係數

3. **Pricing**: 定價表
   - `Media`: 媒體類型
   - `Region`: 區域
   - `List_Price`: 定價
   - `Net_Price`: 實作價
   - `Std_Spots`: 標準檔次
   - `Day_Part`: 時段

4. **Sales**: 業務名單
   - `Name`: 業務姓名
   - `Nickname`: 業務綽號

**回傳值**:
```python
(
    store_counts,        # 店舖數字典 {key: display_name}
    store_counts_num,    # 店舖數字典 {key: count}
    pricing_db,          # 定價資料庫（巢狀字典）
    sec_factors,         # 秒數係數字典 {media: {seconds: factor}}
    sales_map,           # 業務對照表 {name: nickname}
    err_msg              # 錯誤訊息（成功時為 None）
)
```

**快取機制**: 使用 `@st.cache_data(ttl=300)` 快取 5 分鐘

---

### 5. `calculator.py` - 排程運算核心邏輯

**職責**: 執行預算分配與檔次計算

**主要函式**:

#### `calculate_plan_data(config, total_budget, days_count, pricing_db, sec_factors, store_counts_num, regions_order)`

**參數說明**:
- `config`: 媒體設定字典
  ```python
  {
      "全家廣播": {
          "is_national": bool,      # 是否全省聯播
          "regions": list[str],     # 區域列表
          "sec_shares": {int: int}, # 秒數佔比 {秒數: 百分比}
          "share": int              # 預算佔比
      },
      ...
  }
  ```
- `total_budget`: 總預算
- `days_count`: 走期天數
- `pricing_db`: 定價資料庫
- `sec_factors`: 秒數係數
- `store_counts_num`: 店舖數
- `regions_order`: 區域順序

**回傳值**:
```python
(
    rows,              # 計算結果列表
    total_list_accum, # 定價總額
    logs              # 運算過程紀錄
)
```

**計算邏輯**:

1. **預算分配**:
   - 根據媒體佔比計算各媒體預算
   - 根據秒數佔比計算各秒數預算

2. **單檔成本計算**:
   ```
   Unit Cost = (Net Price / Std Spots) × Factor
   ```

3. **檔次計算**:
   ```
   初估檔次 = 預算 / 單檔成本
   最終檔次 = Ceil(預算 / 單檔成本)，再調整為偶數
   ```

4. **特殊邏輯**:
   - 新鮮視非全省投放：標準檔次需加倍
   - 家樂福：量販與超市分開計算，超市依比例換算
   - 檔次需為偶數

#### `render_logic_panel(logs)`

**功能**: 在 UI 上顯示詳細運算邏輯面板（透明化運算過程）

**顯示內容**:
- 分配預算
- 單檔成本
- 秒數係數
- 最終檔次

---

### 6. `html_generator.py` - HTML 預覽生成

**職責**: 生成 HTML 預覽表格

**主要函式**:
- `generate_html_preview(...)`: 生成完整 HTML 預覽

**參數**:
- `rows`: 計算結果
- `days_cnt`: 天數
- `start_dt`, `end_dt`: 日期範圍
- `c_name`, `tax_id`, `p_display`: 客戶資訊
- `format_type`: 格式類型
- `remarks`: 備註列表
- `total_list`, `grand_total`, `budget`, `prod`: 金額資訊

**功能**:
- 生成日期欄位（自動標記週末）
- 根據格式類型切換表頭顏色
- 顯示每日檔次分配
- 顯示總計與備註

---

### 7. `excel_renderer.py` - Excel 報表生成引擎

**職責**: 生成三種格式的 Excel 報表

**主要函式**:
- `generate_excel_from_scratch(...)`: 主函式，根據格式類型調用對應引擎

**支援格式**:

#### 1. 東吳格式 (`render_dongwu_optimized`)
- 標題: "Media Schedule"
- 欄位: Station, Location, Program, Day-part, Size, rate(Net), Package-cost(Net)
- 特色: 簡潔英文格式

#### 2. 聲活格式 (`render_shenghuo_optimized`)
- 標題: "聲活數位-媒體計劃排程表"
- 欄位: 頻道, 播出地區, 播出店數, 播出時間, 秒數/規格, 單價, 金額
- 特色: 中文格式，含公司資訊

#### 3. 鉑霖格式 (`render_bolin_optimized`)
- 標題: "鉑霖行動行銷-媒體計劃排程表"
- 欄位: 頻道, 播出地區, 播出店數, 播出時間, 規格, 單價, 金額
- 特色: 含 Logo，TO/FROM 區塊

**通用功能**:
- 自動計算欄寬列高
- 週末日期標記（黃色背景）
- 合併儲存格（相同媒體、秒數）
- 邊框與樣式設定
- 頁尾（費用、簽名區）

**快取機制**: 使用 `@st.cache_data(ttl=3600)` 快取 1 小時

---

### 8. `pdf_converter.py` - PDF 轉檔功能

**職責**: 將 Excel 轉換為 PDF

**主要函式**:

#### `find_soffice_path()`
尋找系統中的 LibreOffice 執行檔路徑

**支援平台**:
- Linux: `soffice` 或 `libreoffice`
- Windows: 
  - `C:\Program Files\LibreOffice\program\soffice.exe`
  - `C:\Program Files (x86)\LibreOffice\program\soffice.exe`

#### `get_cloud_logo_bytes()`
下載鉑霖 Logo（用於 Excel 報表）

**快取**: `@st.cache_data(ttl=3600)` 快取 1 小時

#### `xlsx_bytes_to_pdf_bytes(xlsx_bytes)`
使用 LibreOffice CLI 將 Excel bytes 轉為 PDF bytes

**流程**:
1. 寫入暫存檔
2. 呼叫 `soffice --headless --convert-to pdf`
3. 讀取 PDF
4. 清除暫存檔

**回傳值**: `(pdf_bytes, method, err_msg)`

**⚠️ 系統需求**: 伺服器需安裝 LibreOffice

---

### 9. `ragic_api.py` - Ragic API 整合

**職責**: 與 Ragic 資料庫進行 CRUD 操作

**主要函式**:

#### `get_ragic_record(api_url, api_key, rid)`
透過 Record ID 讀取單筆資料

**參數**:
- `api_url`: Ragic API URL
- `api_key`: Ragic API Key
- `rid`: Record ID

**重要**: 使用 `naming='EID'` 確保回傳 Field ID 而非欄位名稱

#### `upload_to_ragic(api_url, api_key, data_dict, files_dict)`
上傳資料至 Ragic

**特殊機制**: 上傳成功後會回查流水號（Cue表編號）
- 重試 3 次，每次等待 1.5 秒
- 解析 Ragic 回傳的資料結構
- 取得自動生成的流水號

**回傳值**: `(success: bool, message: str, ragic_id: str)`

#### `search_ragic_records(api_url, api_key, keyword, limit)`
搜尋 Ragic 資料（全文檢索）

**參數**:
- `keyword`: 搜尋關鍵字（Cue號或客戶名稱）
- `limit`: 回傳筆數上限（預設 20）

**回傳值**: 記錄列表（按 ID 降序排列）

#### `restore_state_from_ragic(record)`
從 Ragic Record 還原到 Session State

**功能**:
- 還原基礎欄位（客戶名稱、產品、預算等）
- 解析 Details 字串還原複雜設定（媒體、秒數、區域）
- 還原日期欄位

**回傳值**: `(success: bool, message: str)`

---

## 配置管理

### 重要配置項

#### 1. Ragic 欄位 ID 維護

**位置**: `config.py` → `RAGIC_MAP`

**如何確認欄位 ID**:
1. 在 Streamlit 側邊欄搜尋一筆資料
2. 展開「🛠️ 除錯模式：查看欄位 ID」
3. 對照 JSON 資料找到對應的 Key
4. 更新 `config.py` 中的 `RAGIC_MAP`

**⚠️ 特別注意**: `RAGIC_FIELD_SERIAL` 是 Cue表編號欄位，若變更需要立即更新

#### 2. Google Sheet 設定檔

**位置**: `config.py` → `GSHEET_SHARE_URL`

**Sheet 結構要求**:
- **Stores**: 必須包含 `Key`, `Display_Name`, `Count` 欄位
- **Factors**: 必須包含 `Media`, `Seconds`, `Factor` 欄位
- **Pricing**: 必須包含 `Media`, `Region`, `List_Price`, `Net_Price`, `Std_Spots`, `Day_Part` 欄位
- **Sales**: 必須包含 `Name`, `Nickname` 欄位

**權限**: Sheet 需開啟「知道連結的使用者」可檢視

#### 3. 主管密碼

**位置**: `config.py` → `SUPERVISOR_PASSWORD`

**功能**: 解鎖 Excel 下載與價格覆寫功能

**安全性建議**: 
- 生產環境建議使用環境變數
- 或使用 `.env` 檔案（搭配 `python-dotenv`）

#### 4. Ragic API 設定

**位置**: `config.py` → `DEFAULT_RAGIC_URL`, `DEFAULT_RAGIC_KEY`

**取得方式**:
1. 登入 Ragic
2. 進入表單設定
3. 開啟 API 功能
4. 複製 API URL 與 API Key

---

## 安裝與部署

### 系統需求

- Python 3.8+
- LibreOffice（用於 PDF 轉檔）
- 網路連線（存取 Google Sheet 與 Ragic）

### 安裝步驟

#### 1. 安裝 Python 套件

```bash
pip install -r requirements.txt
```

**套件清單**:
- `streamlit`: Web 框架
- `pandas`: 資料處理
- `openpyxl`: Excel 處理
- `xlsxwriter`: Excel 寫入（備用）
- `requests`: HTTP 請求
- `weasyprint`: PDF 生成（備用，目前使用 LibreOffice）

#### 2. 安裝 LibreOffice

**Windows**:
1. 下載安裝程式: https://www.libreoffice.org/download/
2. 安裝至預設路徑: `C:\Program Files\LibreOffice\`
3. 或安裝至: `C:\Program Files (x86)\LibreOffice\`

**Linux**:
```bash
sudo apt-get install libreoffice
# 或
sudo yum install libreoffice
```

#### 3. 設定配置檔

編輯 `config.py`:
- 確認 `GSHEET_SHARE_URL` 正確
- 設定 `DEFAULT_RAGIC_URL` 與 `DEFAULT_RAGIC_KEY`
- 確認 `RAGIC_MAP` 欄位 ID 正確
- 設定 `SUPERVISOR_PASSWORD`

#### 4. 執行應用程式

```bash
streamlit run app.py
```

應用程式會在 `http://localhost:8501` 啟動

### 部署至 Streamlit Cloud

1. 將程式碼推送到 GitHub
2. 登入 https://streamlit.io/cloud
3. 建立新應用程式
4. 選擇 Repository
5. 設定 Main file path: `app.py`
6. 確認 `requirements.txt` 存在

**⚠️ 注意**: Streamlit Cloud 不支援 LibreOffice，PDF 轉檔功能會失效

### 部署至自有伺服器

#### 使用 systemd (Linux)

建立服務檔案 `/etc/systemd/system/cueapp.service`:

```ini
[Unit]
Description=Cue Sheet Pro Streamlit App
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/cueapp
Environment="PATH=/usr/bin:/usr/local/bin"
ExecStart=/usr/bin/streamlit run app.py --server.port=8501 --server.address=0.0.0.0
Restart=always

[Install]
WantedBy=multi-user.target
```

啟動服務:
```bash
sudo systemctl enable cueapp
sudo systemctl start cueapp
```

#### 使用 Nginx 反向代理

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## 資料流程

### 完整流程圖

```
1. 使用者輸入
   ├─ 客戶資訊（名稱、統編、產品）
   ├─ 預算與製作費
   ├─ 日期範圍
   └─ 媒體設定（全家廣播、新鮮視、家樂福）

2. 載入設定檔 (data_loader.py)
   └─ 從 Google Sheet 讀取
      ├─ Stores（店舖數）
      ├─ Factors（秒數係數）
      ├─ Pricing（定價）
      └─ Sales（業務名單）

3. 排程計算 (calculator.py)
   ├─ 預算分配
   ├─ 單檔成本計算
   ├─ 檔次計算
   ├─ 檔次計算
   └─ 排程分配

4. 生成報表
   ├─ HTML 預覽 (html_generator.py)
   ├─ Excel 報表 (excel_renderer.py)
   └─ PDF 轉檔 (pdf_converter.py)

5. 上傳至 Ragic (ragic_api.py)
   ├─ 準備資料 payload
   ├─ 準備檔案 payload
   ├─ 執行上傳
   └─ 回查流水號
```

### 資料結構

#### 媒體設定 (config)
```python
{
    "全家廣播": {
        "is_national": True,           # 是否全省聯播
        "regions": ["全省"],           # 區域列表
        "sec_shares": {20: 100},      # 秒數佔比 {秒數: 百分比}
        "share": 50                    # 預算佔比
    },
    "新鮮視": {
        "is_national": False,
        "regions": ["北區", "中區"],
        "sec_shares": {10: 50, 20: 50},
        "share": 30
    },
    "家樂福": {
        "regions": ["全省"],
        "sec_shares": {20: 100},
        "share": 20
    }
}
```

#### 計算結果 (rows)
```python
[
    {
        "media": "全家廣播",
        "region": "北區",
        "program_num": 500,           # 店舖數
        "daypart": "全天",
        "seconds": 20,
        "spots": 100,                  # 總檔次
        "schedule": [10, 10, 10, ...], # 每日檔次分配
        "rate_display": 50000,         # 單價
        "pkg_display": 5000000,        # 總金額
        "is_pkg_member": False,        # 是否為打包價格
        "nat_pkg_display": 0           # 全省打包價格
    },
    ...
]
```

---

## 維護指南

### 日常維護

#### 1. 檢查 Google Sheet 設定檔

**頻率**: 每月或當定價變更時

**檢查項目**:
- Stores 表：店舖數是否更新
- Factors 表：秒數係數是否正確
- Pricing 表：定價是否最新
- Sales 表：業務名單是否完整

#### 2. 檢查 Ragic 連線

**頻率**: 每週

**檢查方式**:
- 在側邊欄搜尋一筆資料
- 確認能正常讀取與上傳

#### 3. 檢查 LibreOffice

**頻率**: 每月

**檢查方式**:
- 生成一份 PDF
- 確認轉檔成功

### 常見維護任務

#### 更新 Ragic 欄位 ID

**情境**: Ragic 表單設計變更，欄位 ID 改變

**步驟**:
1. 在 Streamlit 側邊欄搜尋一筆資料
2. 展開「🛠️ 除錯模式：查看欄位 ID」
3. 對照 JSON 找到新的欄位 ID
4. 更新 `config.py` 中的 `RAGIC_MAP`
5. 特別注意 `RAGIC_FIELD_SERIAL`（Cue表編號）

#### 更新定價

**情境**: 媒體定價變更

**步驟**:
1. 開啟 Google Sheet
2. 編輯 **Pricing** 表
3. 更新對應的 `List_Price` 與 `Net_Price`
4. 確認 `Std_Spots`（標準檔次）正確
5. 清除 Streamlit 快取（側邊欄「🧹 清除快取」）

#### 新增業務

**情境**: 新增業務人員

**步驟**:
1. 開啟 Google Sheet
2. 編輯 **Sales** 表
3. 新增一列：`Name`（姓名）、`Nickname`（綽號）
4. 清除 Streamlit 快取

#### 修改主管密碼

**情境**: 密碼洩露或定期更換

**步驟**:
1. 編輯 `config.py`
2. 修改 `SUPERVISOR_PASSWORD`
3. 重啟應用程式

### 故障排除

#### 問題 1: Google Sheet 讀取失敗

**症狀**: 顯示「❌ 設定檔載入失敗」

**可能原因**:
- Sheet 權限未開啟
- Sheet 結構變更（欄位名稱不符）
- 網路連線問題

**解決方法**:
1. 確認 Sheet 已開啟「知道連結的使用者」可檢視
2. 檢查 Sheet 結構是否符合要求
3. 檢查網路連線
4. 查看錯誤訊息中的詳細資訊

#### 問題 2: PDF 轉檔失敗

**症狀**: 顯示「PDF 生成失敗」

**可能原因**:
- LibreOffice 未安裝
- LibreOffice 路徑錯誤
- 暫存檔權限問題

**解決方法**:
1. 確認 LibreOffice 已安裝
2. 檢查 `pdf_converter.py` 中的路徑設定
3. 確認暫存目錄有寫入權限
4. 查看錯誤訊息

#### 問題 3: Ragic 上傳失敗

**症狀**: 顯示「上傳失敗」

**可能原因**:
- API Key 錯誤
- API URL 錯誤
- 欄位 ID 錯誤
- 檔案大小超過限制

**解決方法**:
1. 確認 `DEFAULT_RAGIC_URL` 與 `DEFAULT_RAGIC_KEY` 正確
2. 檢查 `RAGIC_MAP` 欄位 ID 是否正確
3. 確認檔案大小（Ragic 通常限制 10MB）
4. 查看錯誤訊息中的詳細資訊

#### 問題 4: 計算結果異常

**症狀**: 檔次或金額計算錯誤

**可能原因**:
- Google Sheet 資料錯誤
- 計算邏輯問題
- 快取資料過期

**解決方法**:
1. 檢查 Google Sheet 資料是否正確
2. 清除 Streamlit 快取
3. 查看「🧮 運算邏輯詳細面板」確認計算過程
4. 檢查 `calculator.py` 中的計算邏輯

---

## 開發指南

### 開發環境設定

1. **建立虛擬環境**:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate  # Windows
```

2. **安裝開發套件**:
```bash
pip install -r requirements.txt
```

3. **執行開發伺服器**:
```bash
streamlit run app.py
```

### 程式碼風格

- 遵循 PEP 8
- 使用中文註解（業務邏輯部分）
- 函式需有 docstring
- 變數命名使用有意義的名稱

### 新增功能

#### 新增媒體類型

1. 在 `app.py` 的媒體設定區新增 checkbox
2. 在 `calculator.py` 的 `calculate_plan_data` 中新增計算邏輯
3. 在 `excel_renderer.py` 的渲染函式中新增顯示邏輯
4. 更新 `html_generator.py` 的顯示邏輯

#### 新增報表格式

1. 在 `excel_renderer.py` 中新增 `render_xxx_optimized` 函式
2. 在 `generate_excel_from_scratch` 中新增格式判斷
3. 在 `app.py` 的格式選項中新增選項

### 測試

#### 單元測試建議

建立 `tests/` 目錄，測試各模組功能：

```python
# tests/test_calculator.py
import unittest
from calculator import calculate_plan_data

class TestCalculator(unittest.TestCase):
    def test_budget_allocation(self):
        # 測試預算分配邏輯
        pass
```

#### 整合測試

1. 測試完整流程（輸入 → 計算 → 生成 → 上傳）
2. 測試錯誤處理
3. 測試邊界條件

### 除錯技巧

#### 使用 Streamlit 除錯模式

在 `app.py` 開頭加入：
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

#### 查看 Session State

在 `app.py` 中加入：
```python
st.write(st.session_state)  # 顯示所有 Session State
```

#### 查看計算過程

使用 `render_logic_panel` 函式顯示詳細計算過程

---

## 常見問題

### Q1: 為什麼需要 LibreOffice？

**A**: LibreOffice 用於將 Excel 轉換為 PDF。Streamlit Cloud 不支援 LibreOffice，若部署至 Streamlit Cloud，PDF 功能會失效。

### Q2: 可以改用其他 PDF 轉檔方式嗎？

**A**: 可以，但需要修改 `pdf_converter.py`。可選方案：
- `weasyprint`: 需要將 Excel 轉為 HTML 再轉 PDF（複雜）
- `reportlab`: 需要重新實作報表生成（工作量大）
- 雲端 API: 需要額外費用

### Q3: 為什麼使用 Google Sheet 作為設定檔？

**A**: 
- 方便業務人員直接編輯（無需改程式碼）
- 即時更新（清除快取即可生效）
- 版本控制（Google Sheet 有歷史記錄）

### Q4: 如何備份資料？

**A**: 
- Ragic 資料：Ragic 本身有備份功能
- Google Sheet：Google Drive 自動備份
- 程式碼：建議使用 Git 版本控制

### Q5: 如何提升效能？

**A**: 
- 使用 Streamlit 快取（已實作）
- 減少 Google Sheet 讀取頻率
- 優化 Excel 生成邏輯（已優化）

### Q6: 如何新增欄位到 Ragic？

**A**: 
1. 在 Ragic 表單中新增欄位
2. 取得欄位 ID（透過除錯模式）
3. 在 `config.py` 的 `RAGIC_MAP` 中新增對照
4. 在 `app.py` 的上傳邏輯中新增欄位
5. 在 `ragic_api.py` 的 `restore_state_from_ragic` 中新增還原邏輯

---

## API 參考

### Ragic API

#### 讀取單筆資料
```python
record = get_ragic_record(api_url, api_key, record_id)
```

#### 搜尋資料
```python
records = search_ragic_records(api_url, api_key, keyword="關鍵字", limit=20)
```

#### 上傳資料
```python
success, msg, rid = upload_to_ragic(
    api_url, 
    api_key, 
    data_dict,      # 資料字典 {field_id: value}
    files_dict      # 檔案字典 {field_id: (filename, bytes, mime_type)}
)
```

#### 還原狀態
```python
success, msg = restore_state_from_ragic(record)
```

### Google Sheets API (gviz)

#### 讀取 Sheet
```python
url = f"https://docs.google.com/spreadsheets/d/{file_id}/gviz/tq?tqx=out:csv&sheet={sheet_name}"
df = pd.read_csv(url)
```

---

## 版本歷史

### v2.0 (重構版本)
- 模組化架構
- 集中配置管理
- 改善程式碼可讀性

### v1.0 (原始版本)
- 單一檔案架構
- 基本功能實作

---

## 聯絡資訊

**專案維護者**: [您的姓名/團隊]

**問題回報**: [GitHub Issues / Email]

**文件更新日期**: 2024年

---

## 附錄

### A. 檔案結構總覽

```
cueapp/
├── app.py                 # 587 行 - 主程式
├── config.py              # 119 行 - 配置管理
├── utils.py               # 100 行 - 工具函式
├── data_loader.py         # 74 行 - 資料讀取
├── calculator.py          # 259 行 - 計算邏輯
├── html_generator.py      # 116 行 - HTML 生成
├── excel_renderer.py      # 591 行 - Excel 生成
├── pdf_converter.py       # 77 行 - PDF 轉檔
├── ragic_api.py          # 249 行 - Ragic 整合
├── requirements.txt       # 套件清單
└── 技術文件.md            # 本文件
```

### B. 重要常數參考

| 常數名稱 | 位置 | 說明 |
|---------|------|------|
| `GSHEET_SHARE_URL` | config.py | Google Sheet 連結 |
| `RAGIC_FIELD_SERIAL` | config.py | Cue表編號欄位 ID |
| `RAGIC_MAP` | config.py | 欄位 ID 對照表 |
| `SUPERVISOR_PASSWORD` | config.py | 主管密碼 |
| `REGIONS_ORDER` | config.py | 區域順序 |
| `DURATIONS` | config.py | 可用秒數 |

### C. 關鍵函式索引

| 函式名稱 | 模組 | 說明 |
|---------|------|------|
| `load_config_from_cloud` | data_loader.py | 載入設定檔 |
| `calculate_plan_data` | calculator.py | 排程計算 |
| `generate_excel_from_scratch` | excel_renderer.py | 生成 Excel |
| `xlsx_bytes_to_pdf_bytes` | pdf_converter.py | PDF 轉檔 |
| `upload_to_ragic` | ragic_api.py | 上傳至 Ragic |
| `restore_state_from_ragic` | ragic_api.py | 還原狀態 |

---

## 代理商 CUE（新功能）

### 概述
新增製作模式「**代理商CUE**」，產出三家配合代理商專用格式：**2008傳媒**、**佳聖**、**凱絡**。
特性：平台只有「全家企頻」與「萬家福（量販=萬家福 / 超市=樂家康）」、地區只有全台、
訂價與賣價規則獨立於子公司 CUE、支援凌晨補償與專案回饋、檔次不強制偶數。

### 新增模組
- **`agency_cue.py`**：純計算（不 import streamlit）。實作價、牌價查詢、每日檔次分配、
  凌晨補償四方案、專案回饋、費用區、model 建構。核心函式 `build_agency_model(...)`。
- **`agency_excel.py`**：openpyxl 渲染三版型，`generate_agency_excel(model, made_date) -> bytes`，
  每平台一個工作表、橫向 A4、微軟正黑體、金額會計格式。
- **`agency_ui.py`**：`render_agency_cue()` 表單 + 運算面板 + HTML 預覽（`components.html`）+ Excel/PDF 下載。
- **`tests/test_agency.py`**：驗收測試（不依賴 streamlit），對照範例檔數字（A~F）。

### 計算規則（重點）
- 實作價（賣客戶 Net，線性比例）：全家企頻 `250000/480/30×秒`；萬家福量販 `250000/420/20×秒`；
  超市檔次 = 量販 × 720/420（計價於量販，不另收費）。
- 牌價（表上定價）：見 `config.AGENCY_LIST_PRICE_FALLBACK`；查價順序 Sheet(`AgencyPricing`) → fallback；
  無該秒數時取最接近秒數依線性比例換算。
- 凌晨補償（製表四選一）：原50%併入主時段（預設）／方案一15%換全家／方案二20%換萬家福／無補償。
- 專案回饋：每平台一個 % 輸入（預設 0）；萬家福另有「整波專案回饋」（整張不收費、量販手動輸入）。
- 費用區：2008=Budget→AC%→5%Tax→TOTAL；佳聖=Net→VAT5%→Gross（無 AC）；
  凱絡=Sub-Total→A.C3%(預設免收「-」)→VAT5%→Grand-Total，另列媒體總價值/優惠總價值與三層價格（市場價×0.8、統一價×0.75）。
- 檔次取整一律四捨五入（round half up）。

### 維護：AgencyPricing 分頁
在 `config.GSHEET_SHARE_URL` 的 Google Sheet 新增分頁 **`AgencyPricing`**，欄位：
`Agency | Platform | Seconds | List_Per_Spot`。可直接貼入專案根目錄產生的 `AgencyPricing範本.csv`（30 列）。
`data_loader.load_agency_pricing_from_cloud()` 讀取（cache 300 秒），讀不到或欄位不齊則回傳 None 改用 fallback。
UI 會顯示目前牌價來源（Sheet 或內建 fallback）。

### 注意
- 舊功能（一般CUE／年約季約細CUE／Ragic）不受影響；未改動 `calculator.py` / `excel_renderer.py`。
- 代理商 CUE 模式暫不支援 Ragic 上傳、異動CUE。
- PDF 轉檔沿用 `pdf_converter.xlsx_bytes_to_pdf_bytes`（伺服器需安裝 LibreOffice）。

---

**文件結束**
