# 簡易模式 v2 —— 待老闆確認的 13 條規則

> 以下每一條都是「範本(0902)」與「現有 Google Sheet 設定／舊規則文件」互相矛盾之處。
> 系統一律**照範本做、做成常數**，老闆只要一句話就能改（改哪個常數已標明，都在 `simple_config.py`）。
> 想改哪一條，直接說編號即可。

| # | 項目 | 系統現在怎麼做（照範本） | 另一種說法（現有設定／舊文件） | 要改的話動哪個常數 |
|---|---|---|---|---|
| 1 | 萬家福 30 秒係數 | Google Sheet 值 **1.5**（fidelity 測試用 1.65 對齊範本） | 範本 W 欄公式用 **1.65** | `FACTOR_OVERRIDE` 加 `("家樂福",30):1.65` |
| 2 | 新鮮視播出時段 | 顯示 **06-24** | Pricing 表寫 07:00-23:00 | `DAYPART_OVERRIDE["新鮮視"]` |
| 3 | 2008 萬家福牌價 | **4000×家樂福係數**（10秒 2600） | 舊文件：3000×全家係數（1500） | `AGENCY_LIST_PRICE` |
| 4 | 凱絡 全家牌價 | **3000×全家係數**（20秒 2550） | 舊文件：純線性 100/秒（2000） | `AGENCY_LIST_PRICE` |
| 5 | 凱絡 A.C 3% | **免收，顯示「-」** | 舊：`AGENCY_AC_DEFAULT["凱絡"]=3.0` | `CARAT_AC_FREE=True` |
| 6 | 2008 全家 回饋列 | **15% + 46% 兩列「專案回饋」** | 舊例：50% 併入主列 | `BONUS_2008_FAMILY=[15,46]` |
| 7 | 凱絡 全家 回饋列 | **15% / 40% / 6% 三列** | 舊：50% 併入 + 聲活回饋 | `BONUS_CARAT_FAMILY=[15,40,6]` |
| 8 | 2008 萬家福 10% 回饋 | **另立回饋列**（量販+超市） | 舊文件：折進每日不另立列 | `BONUS_2008_WJF_PCT=10`、`BONUS_2008_WJF_AS_ROWS=True` |
| 9 | 子公司平台預算比例 | 統一 **40 / 60**（萬家福或企頻 40、新鮮視或全家 60） | 範本 33%～48% 浮動；上一版 45/55、60/40、58/42 | `COMBOS[key]["split"]` |
| 10 | 每日排檔分配 | **平均，餘數放最前面幾天** | calculator 兩兩成對；2008 舊例餘數放後 | `simple_model.distribute` |
| 11 | 檔次可為奇數、實收不超預算 | **是**（貪婪填滿到 ≥99%，不超預算） | calculator 進位到偶數（會超預算） | `simple_model.allocate_spots` |
| 12 | 5% VAT 是否含製作費 | **含**（(預算+製作)×5%） | 舊 simple_render 漏製作費 | 已修正（`_build_subsidiary` fees） |
| 13 | 效益欄（曝光/人流量） | **預設隱藏、不進客戶檔案**；人流量係數 1000/144 | 範本隱藏欄（客戶專屬） | `SHOW_REACH_COLUMNS=False`、`TRAFFIC_FACTOR` |

## 另外照範本改正的地方（不需老闆確認，僅告知）
- 範本錯字「中彰投**雲**」「**霊**嘉南」→ 已改正為「中彰投」「雲嘉南」。
- 萬家福/樂家康店數在範本存成文字「62店」且用「面」格式 → 已改為數字＋「店」格式（效益公式才不會錯）。
- 六、日底色（FFFF99）範本用條件格式；系統直接填色，效果相同。
- 含新鮮視的組合，備註第 4 條加註「影片/影像 1920x1080 (mp4)」。
- 內部「計價模式／實作價」欄（320000/480 這類）**一律不寫進客戶 Excel**。

## §區域　指定投放區域（分區 CUE）—— 待老闆確認聚合規則
子公司組合①②③的「全家企頻／新鮮視」區塊，UI 進階區可勾選特定區域（預設全省＝套裝價）。
選特定區域時，該區塊整筆預算集中投入所選區域，檔次以下列規則試算：

| 項目 | 現行做法（待確認） | 備註 |
|---|---|---|
| 分區單檔實作價 | `Σ(各選定區 Net) / 全省 Std × 秒數係數` | Net 用各區牌價**加總**；Std 沿用**全省**實作基準（尊重 §4.6「新鮮視全省 Std=504 為實作價、各區 1008 僅供 rate 顯示」設計，避免用 1008 使檔次翻倍） |
| 全省 vs 全選六區 | 全選六區＝視為全省（走套裝價，非六區牌價加總） | 套裝有量價折扣，à la carte 加總會較貴，此為刻意 |
| 家樂福（萬家福／樂家康） | 不分區，完全不受影響 | Pricing 表本就只有量販_全省／超市_全省 |
| rate(Net) 顯示欄 | 仍用**各區**牌價/Std（不變） | 客戶看到的每檔單價維持分區呈現 |

⚠️ **老闆需確認**：分區時「Net 加總 ÷ 全省 Std」是否為正確計價；或應另有各區套裝價/最低量門檻。改法：僅需調整 `simple_model.sub_unit_net` 的聚合式，UI／版面不動。

### 補充例子：新鮮視「只選北區」檔次與全省相同（資料特性，非程式錯）
新鮮視北區 Net 與全省 Net 在 Pricing 表都是 120,000，且分區實作價採「Σ選定區 Net ÷ 全省 Std」，
故「只投北區」算出的檔次會與「全省」一樣多。這是 Pricing 表的資料特性（北區=全省價），
不是程式錯誤；老闆若認為北區應比全省便宜，需調整 Pricing 表的新鮮視北區 Net。
